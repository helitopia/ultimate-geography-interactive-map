import {commonColors} from "../config";

export default class Region {
    private borderWidth: number = 1;
    private borderColor: string = commonColors.region;
    private fillColor: string = commonColors.border;
}