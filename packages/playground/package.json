{
  "name": "playground",
  "version": "1.0.0",
  "main": "index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "build": "npm run type-check && set NODE_ENV=production&& node esbuild.config.js",
    "debug": "npm run type-check && node esbuild.config.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "esbuild-plugin-copy": "^2.1.1",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "interactive-map": "1.0.0"
  }
}
